server:
  port: 5013

spring:
  resources:
    static-locations: classpath:/templates/
  mvc:
    view:
      suffix: .html

lightweight:
  cluster:
    address: localhost:5014,localhost:5013

logging:
  level:
    com.huang: debug